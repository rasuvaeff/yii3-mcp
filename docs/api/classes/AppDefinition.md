---
title: "AppDefinition"
description: "One MCP App (`io.modelcontextprotocol/ui`): an interactive HTML document served as a `ui://` resource and rendered by the client in a sandboxed iframe."
---

<!-- AUTO-GENERATED by docs/scripts/generate-api.mjs from a reflection pass over src/ (docs/scripts/reflect-api.php) — do not edit this file directly. -->

# `AppDefinition`

`Rasuvaeff\Yii3Mcp\Apps\AppDefinition`

**Class** — [`Rasuvaeff\Yii3Mcp`](/packages/core) — [Source](https://github.com/rasuvaeff/yii3-mcp/blob/master/src/Apps/AppDefinition.php)

One MCP App (`io.modelcontextprotocol/ui`): an interactive HTML document
served as a `ui://` resource and rendered by the client in a sandboxed
iframe.

The HTML is either a fixed string or a `Closure(): string` evaluated on
every `resources/read` — a closure is the hook for templating, DI-provided
data or per-request state, and its cost is paid per read.

`$contentMeta` carries the sandbox contract (CSP allow-lists, permissions,
domain, border preference) and belongs to the resource CONTENT; the
descriptor in `resources/list` only ever carries the extension's marker
(McpApps::resourceMarker()), which McpAppsConfigurator adds.
Leaving it `null` is valid: the host then applies its own restrictive
default policy.

## Properties

| Property | Type | Readonly |
|---|---|---|
| `uri` | `string` | yes |
| `name` | `string` | yes |
| `html` | `Closure|string` | yes |
| `title` | `?string` | yes |
| `description` | `?string` | yes |
| `contentMeta` | `?Mcp\Schema\Extension\Apps\UiResourceContentMeta` | yes |

## Methods

### create()

```php
static create(
    string $uri,
    string $name,
    Closure|string $html,
    ?string $title,
    ?string $description,
    ?Mcp\Schema\Extension\Apps\UiResourceContentMeta $contentMeta,
): Apps\AppDefinition
```

### renderHtml()

```php
renderHtml(): string
```

The markup to serve now: a closure is re-evaluated on every call, a
string is returned as given.

