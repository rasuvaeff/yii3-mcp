---
title: "MarkdownPromptsConfigurator"
description: "Registers every `*.md` file of a directory as an MCP prompt: YAML frontmatter provides the metadata (`name` defaults to the file name), the body is the…"
---

<!-- AUTO-GENERATED by docs/scripts/generate-api.mjs from a reflection pass over src/ (docs/scripts/reflect-api.php) — do not edit this file directly. -->

# `MarkdownPromptsConfigurator`

`Rasuvaeff\Yii3Mcp\Prompts\MarkdownPromptsConfigurator`

**Class** — [`Rasuvaeff\Yii3Mcp`](/packages/core) — [Source](https://github.com/rasuvaeff/yii3-mcp/blob/master/src/Prompts/MarkdownPromptsConfigurator.php)

Registers every `*.md` file of a directory as an MCP prompt: YAML
frontmatter provides the metadata (`name` defaults to the file name),
the body is the prompt text with `{{argument}}` placeholders.

Prompts become content, not code: edited without a deployment and
versioned like any other file.

A missing directory throws at build time; a directory without `*.md`
files (or one the process cannot list) registers no prompts — an empty
prompts directory is a valid state, not an error.

The file format is intentionally compatible with — and inspired by —
https://github.com/vjik/my-prompts-mcp vjik/my-prompts-mcp
by Sergei Predvoditelev.

## Methods

### configure()

```php
configure(Mcp\Server\Builder $builder): void
```

