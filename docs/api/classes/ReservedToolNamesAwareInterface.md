---
title: "ReservedToolNamesAwareInterface"
description: "A ServerConfiguratorInterface that wants to know which tool names the attribute-based tools already occupy, so it can fail fast instead of registering a…"
---

<!-- AUTO-GENERATED by docs/scripts/generate-api.mjs from a reflection pass over src/ (docs/scripts/reflect-api.php) — do not edit this file directly. -->

# `ReservedToolNamesAwareInterface`

`Rasuvaeff\Yii3Mcp\ReservedToolNamesAwareInterface`

**Interface** — [`Rasuvaeff\Yii3Mcp`](/packages/core) — [Source](https://github.com/rasuvaeff/yii3-mcp/blob/master/src/ReservedToolNamesAwareInterface.php)

A ServerConfiguratorInterface that wants to know which tool names the
attribute-based tools already occupy, so it can fail fast instead of
registering a colliding one.

The SDK's registry is last-write-wins and its loaders run explicit
registrations (Builder::add(), used by configurators) BEFORE reflected
ones (Builder::addTool(), used for #[McpTool] methods) — so a configurator
that picks an already-taken name does not overwrite the attribute tool, it
silently loses its own. McpServerFactory hands the reserved names to every
configurator implementing this interface before calling configure().

## Methods

### withReservedToolNames()

```php
withReservedToolNames(array $names): static
```

- `$names` — tool names already registered from #[McpTool] attributes

