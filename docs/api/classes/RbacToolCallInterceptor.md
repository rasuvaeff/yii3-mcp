---
title: "RbacToolCallInterceptor"
description: "Enforces the RBAC permission from the PermissionMap on every tools/call."
---

<!-- AUTO-GENERATED by docs/scripts/generate-api.mjs from a reflection pass over src/ (docs/scripts/reflect-api.php) — do not edit this file directly. -->

# `RbacToolCallInterceptor`

`Rasuvaeff\Yii3McpRbacBridge\RbacToolCallInterceptor`

**Class** — [`Rasuvaeff\Yii3McpRbacBridge`](/packages/rbac-bridge) — [Source](https://github.com/rasuvaeff/yii3-mcp-rbac-bridge/blob/master/src/RbacToolCallInterceptor.php)

Enforces the RBAC permission from the PermissionMap on every
tools/call. Tools without a mapped permission pass through; a mapped tool
requires the current identity to hold the permission — a guest (null id)
is checked as a guest and denied by any sane RBAC setup (fail-closed).

The denial is a regular MCP tool-error envelope, so the agent sees the
reason instead of a transport failure.

## Methods

### intercept()

```php
intercept(Interceptor\ToolCallContext $context, callable $next): mixed
```

