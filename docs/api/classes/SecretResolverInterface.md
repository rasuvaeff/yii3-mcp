---
title: "SecretResolverInterface"
description: "Maps a presented endpoint secret to a client identity — the seam that lets several clients (and several ACTIVE secrets per client, for a rotation window)…"
---

<!-- AUTO-GENERATED by docs/scripts/generate-api.mjs from a reflection pass over src/ (docs/scripts/reflect-api.php) — do not edit this file directly. -->

# `SecretResolverInterface`

`Rasuvaeff\Yii3Mcp\Identity\SecretResolverInterface`

**Interface** — [`Rasuvaeff\Yii3Mcp`](/packages/core) — [Source](https://github.com/rasuvaeff/yii3-mcp/blob/master/src/Identity/SecretResolverInterface.php)

Maps a presented endpoint secret to a client identity — the seam that lets
several clients (and several ACTIVE secrets per client, for a rotation
window) share one MCP endpoint without the middleware knowing where the
secrets live.

Implementations MUST compare secrets in constant time (hash_equals())
and MUST NOT log or embed the presented secret in exceptions.

## Methods

### resolve()

```php
resolve(string $presentedSecret): ?string
```

The client id owning the presented secret, or null when no configured
secret matches (the caller rejects the request fail-closed).

