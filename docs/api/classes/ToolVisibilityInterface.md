---
title: "ToolVisibilityInterface"
description: "Per-session tool visibility: decides which tools THIS session may see and call — based on the session's initialize handshake (`client_info`), tenant or…"
---

<!-- AUTO-GENERATED by docs/scripts/generate-api.mjs from a reflection pass over src/ (docs/scripts/reflect-api.php) — do not edit this file directly. -->

# `ToolVisibilityInterface`

`Rasuvaeff\Yii3Mcp\Visibility\ToolVisibilityInterface`

**Interface** — [`Rasuvaeff\Yii3Mcp`](/packages/core) — [Source](https://github.com/rasuvaeff/yii3-mcp/blob/master/src/Visibility/ToolVisibilityInterface.php)

Per-session tool visibility: decides which tools THIS session may see and
call — based on the session's initialize handshake (`client_info`), tenant
or any other session data. The complement of ConditionalToolInterface,
which gates registration globally at build time.

Applied in two places, consistently: tools/list filters the listing, and
tools/call fail-closed rejects a call to an invisible tool (a client that
guesses a hidden name still cannot call it).

This is an early filter, not a replacement for application-level ACL.

## Methods

### isVisible()

```php
isVisible(Mcp\Schema\Tool $tool, ?Mcp\Server\Session\SessionInterface $session): bool
```

